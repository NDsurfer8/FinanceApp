rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow users to upload their own profile photos
    // Pattern: profile-photos/{filename}
    match /profile-photos/{filename} {
      allow read: if true; // Anyone can read profile photos
      allow write: if request.auth != null 
                   && request.resource.size < 5 * 1024 * 1024 // 5MB limit
                   && request.resource.contentType.matches('image/.*'); // Only images
    }
    
    // Allow users to upload any file to their own folder
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null 
                         && request.auth.uid == userId;
    }
    
    // Default rule - deny all other access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
